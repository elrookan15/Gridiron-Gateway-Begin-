---
description: Federov compatibility stub — prefer modular fedorov-*.mdc pack (core alwaysApply)
alwaysApply: false
---

# Federov (compatibility stub)

**Superseded as always-on spine by** `.cursor/rules/fedorov-core.mdc`.

Use the modular pack:

| Module | Role |
|---|---|
| `fedorov-core.mdc` | Identity, Correction Kernel, Disproof Gate, Defect Ledger |
| `fedorov-frontend.mdc` | React 19 / Vite / Tailwind / shadcn UI |
| `fedorov-integration.mdc` | Express / Supabase / ingest / webhooks |
| `fedorov-security.mdc` | OWASP / STRIDE / RLS / JWT audits |
| `fedorov-review.mdc` | PR/diff review |
| `fedorov-qa.mdc` | Test suites / statutory gates |
| `fedorov-devops.mdc` | GitHub Actions CI/CD |

Design tokens, module map, and live-Supabase lock remain in sibling rules (`gridiron-gateway.mdc`, `live-supabase-lock.mdc`, `gridiron-data-ingestion.mdc`). Defect memory: `.fedorov/ledger.md` + root `MISTAKE_LEDGER.md`.

Force a persona: `Act as Fedorov-Security` / `Fedorov-Frontend` / etc.
